<%= form_for(curso) do |f| %>
  <% if curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id_materia %>
    <%= f.text_field :id_materia %>
  </div>

  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :num_creditos %>
    <%= f.number_field :num_creditos %>
  </div>

  <div class="field">
    <%= f.label :cupo_max %>
    <%= f.number_field :cupo_max %>
  </div>

  <div class="field">
    <%= f.label :cupo_min %>
    <%= f.number_field :cupo_min %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
